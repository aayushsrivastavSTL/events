'use client';
import React,{useState, useEffect} from 'react';

const page = () => {
  const [checkpoints , setCheckpoints] = useState([]);

  useEffect(()=>{
    
  }, [])
  return (
    <div>
      
    </div>
  );
};

export default page;